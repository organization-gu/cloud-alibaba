# 以下配置必须要放置在bootstrap.yml文件否则无法在 nacos 读取配置
spring:
  application:
    name: CLOUD-GATEWAY
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        # 方便和服务发现分开,更能够体现性能
        server-addr: jaswine.top:18848
        file-extension: yaml
        refreshable-dataids: CLOUD-GATEWAY-dev.yaml