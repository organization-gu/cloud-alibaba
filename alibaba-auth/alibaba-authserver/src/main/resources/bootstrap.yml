# 以下配置必须要放置在bootstrap.yml文件否则无法在 nacos 读取配置
spring:
  application:
    name: CLOUD-AUTHSERVER
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        # 方便和服务发现分开,更能够体现性能
        server-addr: guyw.top:8848
        file-extension: yaml
        refreshable-dataids: CLOUD-AUTHSERVER-dev.yaml
        namespace: f96a1264-e19e-44bc-9f7a-8534975ce3bc
